/// <reference types="react" />
import { ValueLabelModel } from "../models";
interface Props {
    selectedOptions: ValueLabelModel[];
    selectedRow: ({ index, style }: {
        index: number;
        style: any;
    }) => JSX.Element;
}
declare const SelectedList: ({ selectedOptions, selectedRow }: Props) => JSX.Element;
export default SelectedList;
