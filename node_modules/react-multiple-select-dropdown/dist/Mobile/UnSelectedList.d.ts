/// <reference types="react" />
import { ValueLabelModel } from "../models";
interface Props {
    unSelectedList: ValueLabelModel[];
    unSelectedRow: ({ index, style }: {
        index: number;
        style: any;
    }) => JSX.Element;
}
declare const UnSelectedList: ({ unSelectedList, unSelectedRow }: Props) => JSX.Element;
export default UnSelectedList;
