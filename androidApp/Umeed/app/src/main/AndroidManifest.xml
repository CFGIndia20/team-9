<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.umeed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.EndUserActivity"></activity>
        <activity android:name=".Activities.AssignedTasks" />
        <activity android:name=".Activities.TaskReviewBoard" />
        <activity android:name=".Activities.DistributeTask" />
        <activity android:name=".Activities.AllotTasks" />
        <activity android:name=".Activities.ManagerOptions" />
        <activity android:name=".Activities.MarkAttendance" />
        <activity
            android:name=".RedirectLogin"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>